enum ClientCommands {
    REGISTER = "/reg",
    LOGIN = "/login",
    LOGOUT = "/logout",
    WHISPER = "/w",
    SUBSCRIBE = "/subscribe",
    UNSUBSCRIBE = "/unsubscribe",
    GROUPCHAT = "/group",
    SEND = "/send",
}

export default ClientCommands;