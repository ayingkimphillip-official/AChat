enum ClientCommands {
    REGISTER = "/reg",
    LOGIN = "/login",
    LOGOUT = "/logout",
    WHISPER = "/w",
    SUBSCRIBE = "/sub",
    UNSUBSCRIBE = "/unsub",
    GROUPCHAT = "/group",
    SEND = "/send",
    RESPOND = "/res",
}

export default ClientCommands;